
tasks:
  - name: frontend
    init: |
      cd frontend
      npm install
      npm run build
    command: |
      export GPFS_ENDPOINT=`gp url 3001`
      npm run dev

  - name: backend
    init: |
      cd backend
      npm install
    command: |
      npm start

vscode:
  extensions:
    - svelte.svelte-vscode